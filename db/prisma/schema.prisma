generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:password@localhost:5432/postgres"
}

model srt {
  id          String @id
  name        String
  description String
  slug        String

  @@map("srts")
}
